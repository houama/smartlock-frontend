import { makeStyles } from '@mui/styles'

const useStyles = makeStyles( theme => ({
    container: {
        display: 'flex'
    },
    content: {
        width: '100%'
    }
}))

export default useStyles